{
  "version": 1591,
  "version_name": "1.0",
  "url": "https://github.com/larkdesk/qfam-release/releases/download/v1591/qfam-v1591.apk",
  "notes": "Performance fix for occasional ANR/slow app start: the DB schema-migration check that runs on every app open was re-querying SQLite from scratch for each of ~285 individual column checks instead of caching the table schema once. Also removed a completely unused JobManager library that was still synchronously opening its own SQLite file on every cold start, and moved one more synchronous settings read off the main thread during app startup."
}