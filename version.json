{
  "version": 1584,
  "version_name": "1.0",
  "url": "https://github.com/larkdesk/qfam-release/releases/download/v1584/qfam-v1584.apk",
  "notes": "Fix after-hours exit guard checking the wrong signal: it was gated on the strategy's CURRENT Alpaca auto-trade toggle instead of whether the position actually has a real Alpaca order behind it, so a position bought while linked could still sell after hours once the toggle was later turned off, regardless of allow_after_hours."
}